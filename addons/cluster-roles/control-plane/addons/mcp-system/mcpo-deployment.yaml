# MCPO: MCP-to-OpenAPI proxy
# Wraps MCP servers (prometheus, kubernetes) as OpenAPI endpoints
# Open WebUI connects here via External Tools (OpenAPI type)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcpo
  namespace: mcp-system
  labels:
    app: mcpo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcpo
  template:
    metadata:
      labels:
        app: mcpo
    spec:
      # Deferrable: developer tooling, not a platform dependency.
      priorityClassName: platform-batch
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      initContainers:
        - name: render-config
          # First-party, but :latest drifts on every CI build of the image.
          image: ghcr.io/jamesatintegratnio/gitops_homelab_2_0/kubectl:v1.34.1@sha256:2de503a9fc20271a9981abf91f1e1bbbd11ebd4dc461a046c97e6ca04fd44cdb
          command:
            - /bin/bash
            - -c
            - |
              # Substitute env vars in config template
              sed "s|\${GITHUB_TOKEN}|${GITHUB_TOKEN}|g" /config-template/config.json > /config-rendered/config.json
          env:
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mcp-github-token
                  key: GITHUB_TOKEN
          volumeMounts:
            - name: config-template
              mountPath: /config-template
              readOnly: true
            - name: config-rendered
              mountPath: /config-rendered
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      containers:
        - name: mcpo
          image: ghcr.io/open-webui/mcpo:main@sha256:1e82c9555c19e50b80745705f32b47a2647589f35279527b5118ecd3a71bd467
          args:
            - "--config"
            - "/config-rendered/config.json"
            - "--port"
            - "8000"
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            # Writable home for uvx cache
            - name: HOME
              value: /tmp/mcpo-home
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /openapi.json
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /openapi.json
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /openapi.json
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
          volumeMounts:
            - name: config-rendered
              mountPath: /config-rendered
              readOnly: true
            - name: mcpo-home
              mountPath: /tmp/mcpo-home
      volumes:
        - name: config-template
          configMap:
            name: mcpo-config
        - name: config-rendered
          emptyDir:
            sizeLimit: 1Mi
        - name: mcpo-home
          emptyDir:
            sizeLimit: 500Mi
---
apiVersion: v1
kind: Service
metadata:
  name: mcpo
  namespace: mcp-system
  labels:
    app: mcpo
spec:
  type: ClusterIP
  selector:
    app: mcpo
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
