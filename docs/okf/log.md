# Update Log

## 2026-08-22
* **Update**: etcd defragmented on all three members (non-leaders first, leader `10.0.4.103` last): 584/628/635 MB on disk → 135/135/134 MB, 100% in use, no leader change. Recorded in [known issues](cluster/known-issues.md); the WAL fsync latency is unchanged — that remains the shared-disk problem.
* **Update**: Three of the four Talos patches are **applied** on all nodes (watchdog, `kubernetesTalosAPIAccess`, apiserver toleration defaults; `dns.yaml` withdrawn) — no reboots, one ~45s VIP blip while 101's apiserver restarted. `platform-backups-talos` switched on, so both clusters' etcd are now snapshotted nightly. [Resilience](platform/resilience.md), [backups](platform/backups.md) and [known issues](cluster/known-issues.md) updated; one new caveat — vcluster-synced pods keep the 300s tolerations from the vcluster's own apiserver.
* **Update**: Triaged Kargo's 16 open day-one bump PRs and merged 12; four are held. Four of the twelve only merged safely because a config change went in alongside the bump, each a case where the PR renders fine and breaks at runtime: argo-cd chart 10.0.0 flips `global.networkPolicy.create` on (pinned off — the `network-policies` addon plus Kyverno's default-deny own that, and the render diff against 9.4.3 is now empty); MetalLB 0.16 swaps the speaker's FRR sidecars for a whole frr-k8s DaemonSet, four CRDs and a webhook (both toggles pinned off, this cluster is L2-only) and moved metrics 7472 -> 9120 on both components, which the netpol still had to learn or scraping would have gone dark silently; argocd-mcp v0.9.0 now binds loopback and refuses to widen without a token or `--allow-unauthenticated` (its probes are tcpSocket on localhost, so it would have reported Healthy while serving nothing); and NGF 2.6 requires Gateway API 1.5, so [`gateway-api-crds`](addons/addon-inventory.md) moved v1.4.0 -> v1.5.1 — safe without a storage migration because `httproutes`/`gateways`/`gatewayclasses` already stored `v1` and v1.5.1 still serves v1beta1, keeping ReferenceGrant's v1beta1 as storage. Verified live: all rolled out, gateway Programmed on 10.0.4.205 with argocd/grafana/kargo/auth answering, MetalLB speaker down to one container, and zero chart-authored NetworkPolicies in `argocd`. [Component versions](cluster/component-versions.md) re-measured. One bump was reverted the same night: authentik 2026.8.0 refuses to migrate across major.minor releases in one hop (`ensure_allowed_version()` raises before `run_migrations()`), so its pods took the database lock, refused, released it and crashlooped while the 2025.12.4 pods kept serving — nothing migrated and SSO never went down. Pin restored to 2025.12.4, and Kargo's authentik target constrained to `<2026.3.0` with `autoMerge: never` so it proposes the next legal step (2026.2.3) rather than an impossible jump. Those held upgrades, and this one, are recorded in [known issues](cluster/known-issues.md).
* **Correction**: With a talosconfig finally on the workstation, the Talos patches were dry-run against the live nodes. `dns.yaml` is **withdrawn** — the nodes already resolve via `1.1.1.1`/`8.8.8.8` (both healthy) and `10.0.0.1` is a split-horizon dnsmasq, so the single upstream is the Talos host-DNS proxy (`forwardKubeDNSToHost`), not a router; recorded in [known issues](cluster/known-issues.md). `all.yaml`/`cp.yaml` duplicate list items under `patch mc`; the runbook now applies the two new keys as JSON patches. First tenant etcd snapshot verified on the share (33 MB); [backups](platform/backups.md) promoted to `stable`. The live `patch mc` is still a human step (agent permission layer).

## 2026-08-21
* **Creation**: Added [backups](platform/backups.md) — the recovery layer the seven healing layers lacked. Nightly `etcdctl` snapshots of vcluster-media's etcd (addon `platform-backups`, live on merge) and `talosctl etcd snapshot` of the host etcd (addon `platform-backups-talos`, shipped **disabled** until `all.yaml` is applied — the `talos.dev` ServiceAccount kind does not exist before then), both onto `10.0.0.12:/mnt/user/kube_storage/backups/` with 14-day retention, plus a `platform.backups` alert group and restore runbooks in [operations](../operations.md). In the same change: a PDB for the `vcluster-media-etcd` StatefulSet (the promise only covers the vcluster pods), `KubeCPUOvercommit` un-muted now that it is the N+1 early warning, and the completed 2026-08-21 handoff document removed. New finding recorded in [known issues](cluster/known-issues.md): host etcd is ~630 MB on disk with ~114 MB in use — 82% fragmentation left by the event storm, fixable with `talosctl etcd defrag`.
* **Update**: [Kargo](platform/kargo.md) is live (PR #24 merged 19:42Z). First ten minutes: 48 Warehouses, 39 Freight, 34 PRs opened, 15 self-merged under the per-target policy, 19 waiting for a human. Two day-one defects fixed in the follow-up PR: the Kargo webhook normalises `interval` durations and defaults `freightCreationPolicy`, which ArgoCD read as drift on every Warehouse (seven re-syncs in ten minutes); and chart subscriptions without a constraint selected prereleases (`open-webui 16.0.1-dev.203.1`, `cilium 1.21.0-pre.0` — both manual-merge, nothing landed). A third defect surfaced once the first PRs had merged: verification AnalysisRuns never ran, because Kargo only *creates* them and the first design had installed no Argo Rollouts controller — replaced the CRD-only addon with the `argo-rollouts` chart. Also: the UI was unreachable until the nginx-gateway egress allow-list learned the `kargo` namespace, and 13 Warehouses kept drifting on a webhook-defaulted `strictSemvers`. Concept promoted to `stable`; three of its four assumptions verified, one premise (Kargo runs the analysis itself) disproved. By 20:21Z, with #61 and #66 merged: all four Kargo/Rollouts apps Synced/Healthy, Warehouse drift 0, UI reachable, and 25 of 26 verification AnalysisRuns Successful (5/5 measurements each).
* **Update**: The VCO write loop is **resolved** — the manual-reconciliation label was applied at ~18:46Z, Kratix re-ran the pipeline, rewrote `ConfigureWorkflowCompleted`/`Reconciled`, removed the label, and VCO PUT/s went from ~10 to 0. PR #23 landed as nine linear commits (all apps Synced/Healthy, Kratix image unchanged, cleanup CronJob live, hello-world gone); PR #25 bumps the status-reconciler pin to `main-55400cb` to deploy the guard fix. See [known issues](cluster/known-issues.md).
* **Update**: Fixed the three defects found while chasing the write loop, on one branch with the earlier docs (PR #23): the status-reconciler's `statusUnchanged` guard now applies the merge patch before comparing (the `unhealthy: null` delete semantics were the cause), the Kratix controller image is digest-pinned in values, and the `kratix-pipeline-cleanup` CronJob — which reported success nightly while deleting nothing — is rewritten without `jq` on the repo's pinned kubectl image with `pipefail`. See [known issues](cluster/known-issues.md).
* **Update**: Removed the `hello-world` HTTPService (PR #20) — it was the http-service promise's smoke test and cost three pipelines re-running every ~10h on Kratix's reconciliation interval. Cleared it from the [workload inventory](cluster/workload-inventory.md), [component versions](cluster/component-versions.md), [networking](platform/networking.md), and the [http-service](promises/http-service.md) / [gateway-route](promises/gateway-route.md) promise concepts.
* **Correction**: The VCO write loop is **not** the manual-reconciliation annotation — removing it changed nothing. Measured: 100% of ~10 PUT/s are Kratix itself flipping `workflowsSucceeded` 0↔1. Root cause from Kratix source: the `ConfigureWorkflowCompleted` condition is missing on the VCO (wiped by the pre-fix status-reconciler, which replaced the conditions list for six months), so Kratix's controller computes 0 while its workflow reconciler computes 1, forever. Also stopped the VCO's 10h interval re-runs. Fix is a pipeline re-run via the manual-reconciliation **label**. Two more defects recorded: the reconciler's `statusUnchanged` guard does not work, and the Kratix chart `0.0.1` is effectively unpinned. See [known issues](cluster/known-issues.md).
* **Update**: Found why `mcp-system` stayed broken after both its fixes merged: ArgoCD's in-flight sync operation hard-pins the revision it applies, and `retry.limit: -1` (inherited by **53 of 59** Applications) means it never reaches a terminal state — so no newer commit can ever be applied and the app is wedged against its own fix. Recorded in [known issues](cluster/known-issues.md) with the out-of-band recovery (`terminate-op`), plus the open question of whether the inherited default should become finite.
* **Creation**: Added [Kargo version updates](platform/kargo.md) — Kargo 1.11.2 installed as the `kargo` addon (control-plane only, OCI chart, Authentik PKCE login, break-glass admin from 1Password) plus a local `kargo-projects` factory chart that renders 3 Projects and 48 Warehouse/Stage pairs from one target list covering every pinned image and chart in `addons/`, `promises/` and `workloads/`, with post-merge verification (an Argo Rollouts AnalysisTemplate over `argocd_app_info`; only the three analysis CRDs are installed) for the `addons` and `promises` projects. Each Stage opens a PR against `main` and merges it under a per-target policy (`patch` for CNI/ArgoCD/Kyverno/certs/gateway, `minor` for apps, `always` for our own `main-<sha>` images, `never` for digest-only `mcpo`). Two multi-document Job files were reordered so their Job is the first document — Kargo's `yaml-update` only parses that one. Authored on a branch; the concept is `draft` until someone sees the first sync. Implementation reference: Tensure's private `gitops-apps-config` chart-updater pipelines, which run the same Kargo version.
* **Update**: PRs #16 and #18 merged. #16 did not clear `mcp-system` — the sync moved on to failing at `Deployment/grafana-mcp exceeded its progress deadline`, readiness 403. Root-caused to mcp-grafana 1.1.0's new Host-header validation (`forbidden: host not allowed`), **not** the predicted Grafana Viewer-vs-Admin authorization problem: `--allowed-hosts` defaults to loopback variants of `--address`, so the kubelet probe, mcpo and the Gateway were all refused. Fixed in [ai-stack](platform/ai-stack.md) and the addon by allowlisting the real callers plus an explicit probe `Host` header. Third distinct failure traceable to the one unpinned-tag 0.14.0 → 1.1.0 jump.
* **Update**: Re-measured the open handoff items. Everything from 2026-08-21T17:00Z still holds unchanged — the Kratix `manual-reconciliation` annotation is still set (VCO 8.69 PUT/s, events 8.71/s), PR #16 is still open and `mcp-system` still `OutOfSync/Degraded`, N+1 margin still −1.094. Turned the CoreDNS "single home router upstream" hypothesis into a measurement by scraping both replicas on `:9153`: exactly one forward target, ~2,000 upstream health-check failures and ~4,200–4,700 forwards with no healthy upstream, so "upstream unhealthy" and "no upstream" are the same event. Queued [`dns.yaml`](../../matchbox/talos-machineconfigs/dns.yaml) as the fix (unapplied — needs `talosctl`, and carries two pre-checks) and updated [known issues](cluster/known-issues.md). Corrected the handoff's claim that `Build Go SDK Promise Pipelines` has been failing since 2026-03-06: main is green, the failures are all on the abandoned `desloppify` branch.
* **Update**: Added repository-level agent guidance — a root `CLAUDE.md` operating manual, a full [MCP servers reference](../mcp.md), and `.gitignore` coverage for agent worktrees. Corrected the [AI stack](platform/ai-stack.md) concept: `mcp-system` is an addon now, not an out-of-band Application, and its images are digest-pinned. Marked the retired SSE-era `addons/clusters/the-cluster/addons/mcp-tools/README.md` as superseded.
* **Update**: Post-merge validation of the self-healing rollout surfaced two linked problems, recorded in [known issues](cluster/known-issues.md): etcd is well outside its operating envelope (178ms p99 fsync on talos-xez-xys, 44 leader changes/24h, `/var` sharing one disk with everything), and a status write loop between platform-status-reconciler and Kratix was producing 97% of all cluster events at ~9.5 writes/second. The loop is fixed; the disk is a Talos/hardware change.
* **Creation**: Added [resilience](platform/resilience.md) — the seven self-healing layers, the automation rule (restart/delete yes, desired-state no), the N+1 arithmetic, and what stays outside git's reach.
* **Update**: Root-caused the vcluster-media restart storms to Talos host-DNS stalls saturating CoreDNS, with the Loki correlation recorded in [known issues](cluster/known-issues.md); recorded the resilience change set from branch `claude/self-healing-clusters-97da62` / PR #8. Corrected the [addon system](addons/addon-system.md) sharp edge: addon `syncPolicy` now deep-merges rather than replacing.
* **Update**: Follow-up finding — the `*.cluster.integratn.tech` wildcard certificate had been served **expired since 2026-08-09** because cert-manager v1.16.2's Cloudflare DNS01 cleanup bug blocked renewal; fixed by upgrading the host cert-manager to v1.19.3 (PR #5) and re-issuing (valid to 2026-11-19). Documented in [known issues](cluster/known-issues.md); [component versions](cluster/component-versions.md) updated. All 54 ArgoCD apps now Synced and Healthy.
* **Update**: Quick wins completed end-to-end after PR #2 and #3 merged: Kyverno generate-policy swapped via `kubectl replace --force` (24/24 default-deny coverage preserved, zero gaps), mcp-system adopted by the new addon app and the out-of-band Application retired, llmkube runtime fully torn down (~100Gi reclaimed), etcd-secret-writer re-ran on alpine/k8s, and the state reconciler's GatewayRoute label churn ignored (PR #3). Final state: 54/54 apps Synced; [known issues](cluster/known-issues.md) remediation section updated to reflect completion.
* **Update**: Applied the quick wins from [known issues](cluster/known-issues.md): 6 GitOps commits on `claude/repo-cluster-learning-8dcc81` (pre-commit hook fix, llmkube/git-indexer config removal, mcp-system addon adoption + secret-ref fix, etcd-secret-writer applyability fix, GatewayRoute ignoreDifferences, generate-policy unblock) plus out-of-band deletion of orphans (git-indexer stack, stuck trivy jobs, stale suspended VCO pipeline job). Trivy scanning and the vcluster-media status contract recovered immediately; remaining steps execute after the branch merges to main. Added a remediation-status section to the known-issues concept.

## 2026-08-20
* **Creation**: Initial population of the bundle from a deep review of the repository and a live sweep of `the-cluster` (kubectl, context `admin@the-cluster`). Added 25 concepts across [platform/](platform/index.md), [cluster/](cluster/index.md), [addons/](addons/index.md), [promises/](promises/index.md), [infrastructure/](infrastructure/index.md), and [tooling/](tooling/index.md), including dated snapshots ([workload inventory](cluster/workload-inventory.md), [component versions](cluster/component-versions.md)) and a consolidated [known issues & drift](cluster/known-issues.md) report.
* **Creation**: Scaffolded the GitOps Homelab 2.0 Knowledge Bundle with `okf_init.py` — see [getting started](getting-started.md).
