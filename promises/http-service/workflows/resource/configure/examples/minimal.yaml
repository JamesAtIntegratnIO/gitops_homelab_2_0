# ── Minimal example ──────────────────────────────────────
# Just an image and a name — everything else is defaulted.
apiVersion: platform.integratn.tech/v1alpha1
kind: HTTPService
metadata:
  name: hello-world
  namespace: platform-requests
spec:
  name: hello-world
  image:
    repository: docker.io/nginxdemos/hello
    tag: latest
