useValuesFilePrefix: false
repoURLGitRevision: HEAD
repoURLGitBasePath: "addons"
# only deploy apps to the control-plane environment
globalSelectors:
  environment: "development"
syncPolicy:
  automated:
    selfHeal: false
    allowEmpty: true
    prune: true
syncPolicyAppSet:
    preserveResourcesOnDeletion: true
